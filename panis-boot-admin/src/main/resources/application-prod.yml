spring:
  datasource:
    url: jdbc:mysql://localhost:3306/panis_boot?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 1
      host: localhost
      password:
      connect-timeout: 10000ms
      lettuce:
        pool:
          enabled: true
oss:
  name: minio
  bucket-name: panis
  endpoint: "http://localhost:9000"  # API 端口
  access-key: "minioadmin"           # 用户名
  secret-key: "minioadmin"           # 密码
  sync-delete: true
  expiry: 3600